# Extension icons

Placeholder directory. Add `icon16.png`, `icon48.png`, and `icon128.png` here
before shipping. The manifest references these paths directly; Vite + crxjs
copy them into the bundle.

For local development the extension loads without real icons — Chrome falls
back to a blank square.
